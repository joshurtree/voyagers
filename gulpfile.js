const { dest, parallel, src, task } = require('gulp');

/*
task('create-translation-file', function () {
  src('public/translations/*.json')
    .pipe()
});

task('') {
 return src('src//**//*.ts**')
  .pipe(findTrStrings())
}
*/
